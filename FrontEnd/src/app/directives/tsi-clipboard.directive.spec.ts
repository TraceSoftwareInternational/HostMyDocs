import { TsiClipboardDirective } from './tsi-clipboard.directive';

describe('TsiClipboardDirective', () => {
  it('should create an instance', () => {
    const directive = new TsiClipboardDirective();
    expect(directive).toBeTruthy();
  });
});

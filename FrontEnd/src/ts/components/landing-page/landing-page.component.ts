import { Component } from '@angular/core';

/**
 * This component is displayed when users land on the home page of the app
 */
@Component({
    selector: 'landing-page',
    templateUrl: './template.html',
    styleUrls: ['./styles.sass'],
})
export class LandingPage { }
